\documentclass{article}
\usepackage[numbers]{natbib}
\begin{document}
Given a training dataset, the goal of dataset distillation is to derive a synthetic dataset such that models trained on the latter perform as well as those trained on the training dataset.~\citep{ref_a}.
\bibliographystyle{plainnat}
\bibliography{references}
\end{document}
